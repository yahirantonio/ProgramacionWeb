export default class Cajas{
	Caja1(movimientos){
		console.log(`Entro en la caja 1, el cliente tiene ${movimientos} movimientos`);
	}

	Caja2(movimientos){
		console.log(`Entro en la caja 2, el cliente tiene ${movimientos} movimientos`);
	}

	Caja3(movimientos){
		console.log(`Entro en la caja 3, el cliente tiene ${movimientos} movimientos`);
	}

	Caja4(movimientos){
		console.log(`Entro en la caja 4, el cliente tiene ${movimientos} movimientos`);
	}

	CajaExpress(movimientos){
		console.log(`Entro en la caja express, el cliente tiene ${movimientos} movimiento`);
	}
}